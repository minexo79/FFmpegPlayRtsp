<Window x:Class="FFmpegPlayRtspDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FFmpegPlayRtspDemo"
        mc:Ignorable="d"
        Title="FFmpeg播放" Height="720" Width="1280" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid x:Name="wp">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <local:FFmpegPlayer x:Name="player1" Grid.Column="0" Grid.Row="0" />
            <local:FFmpegPlayer x:Name="player2" Grid.Column="1" Grid.Row="0" />
        </Grid>
    </Grid>
</Window>
